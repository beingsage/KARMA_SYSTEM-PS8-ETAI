# Example locked dependencies to create a reproducible ML environment.
# Adjust CUDA tag (cu118/cu128/etc) to match your system. Use this as a starting point
# and validate the versions against your target deployment.

# PyTorch trio (must match ABI)
torch==2.10.0+cu128
torchvision==0.25.0+cu128
torchaudio==2.10.0

# Transformers and sentence-transformers
transformers==4.36.2
sentence-transformers==2.2.2

# Core libraries used by pipeline
pyarrow==15.0.2
numpy==1.26.4
Pillow==10.4.0
requests==2.31.0

# NLP / topic modelling
bertopic==0.15.0
hdbscan==0.8.30
umap-learn==0.5.4

# Domain-specific
docling==0.8.0
gliner==0.2.0
glirel==0.1.0

# Vector stores and clients
qdrant-client==1.9.6
