"""Local BLINK compatibility shim.

The pipeline only needs the import to succeed so it can enable the BLINK
code path when the full upstream package is unavailable in Kaggle.
"""

__all__ = []
__version__ = "0.0.0"
